export const metadata = {
  title: "Blog | REEDS",
  description: "Read the latest stories, updates, and impact reports from REEDS.",
};

export default function BlogsLayout({ children }) {
  return children;
}
